# CLAUDE.md — TDC Health

Context pack for AI-assisted development. Read fully before any task. This file is the source of truth for locked decisions (names, IDs, stack, domains, banned/approved phrases, data model) — if any other doc, chat, or generated code conflicts with it on these specifics, this file wins. For cross-cutting principles and the reasoning behind them (why Home stays quiet, why a preview must share its data source with the real thing, why Fastlane is a separate service, etc.), see `.specify/memory/constitution.md` — that file is authoritative on principles the same way this one is on concrete decisions. Last updated: 15 Aug 2026.

## What we're building

TDC Health: family personal-health-records Android/web app by The Doorstep Clinic (Satkrut Ventures Pvt. Ltd., Pune). Core user: urban family caregiver ("Rohan, 34") managing elderly parents' health. Hook: NFC emergency card. Retention: records timeline + med reminders.

**Current phase: one project, built to production (owner decision, 6 Sep 2026).** The Track A / Track B split is **abolished** — see constitution v3.0.0 §One Standard. There is no demo track, no deferral to a later track, and no `[A]`/`[B→A]`/`[B]` binding class. Every principle binds every feature; every feature is a priority and owns a complete spec-kit set (`spec.md` + `plan.md` + `tasks.md` + checklists).

**Production quality, demo data switchable.** The prototype survives as a single `DEMO_MODE` switch: **off in every build that leaves a dev machine**, may substitute *data* but never *behaviour* (never skips authorization, logging, validation, or invents a state the real system cannot produce), and may never be the only path — if removing it breaks a feature, the feature is unfinished. Edge cases now have to work, not merely not crash.

**Capacity is unchanged: solo dev, ~4h/day.** So "every feature is a priority" means *none is cut to fund another* — the old P0/P1 ladder and cut-from-the-bottom slip rule are repealed. It does **not** mean everything at once: features ship in **dependency order**, one at a time, each to completion.

**Production ≠ every integration live.** ABDM, UHI and payment rails sit behind external gates this project doesn't control (`docs/compliance-baseline.md`: WASA is a precondition for ABDM M1; production ABDM is four gates out). A feature blocked on such a gate names **the gate and its owner**, ships everything on our side of it, and still may not claim the integration exists (Principle VI).

Full specs (Spec Kit format) in `specs/`: `001-tdc-phr-patient-app` (core app spec, includes §4.1 UX loops) · `002-onboarding-router-activation` (onboarding, emergency card, ABHA activation) · `003-abdm-sync-subscription` (ABDM continuous sync — gated on ABDM certification) · `004-seed-data-and-summary` (seed/fixture dataset + PDF field mapping) ·
`012-appointment-booking` (**the live booking spec** — discovery, slots,
confirm, lifecycle, reminders; supersedes `010`+`011`, which are retained as
decision records only).

## Locked decisions — never revisit, never regenerate alternatives

- **Names:** patient app = **TDC Health** · doctor app = TDC Doctor (name frozen; not started) · HMS = TDC Clinic (CARE fork) · admin = TDC Console · card = TDC Emergency Card. First public mention always "The Doorstep Clinic (TDC)".
- **Android package IDs (permanent):** `in.thedoorstepclinic.health` · `in.thedoorstepclinic.doctor`
- **Stack (Python-only backend):** Flutter (Android + web, one codebase) · TDC Core API = Django/DRF + SimpleJWT + Postgres · Emergency Fastlane = separate FastAPI service · Celery for async · FCM (direct) · Razorpay UPI-first (in scope — see Payments below) · DigitalOcean Bangalore droplet.
- **Repos:** `thedoorstepclinic/platform` (this monorepo, private) · `thedoorstepclinic/tdc-care` (TDC Clinic — CARE fork, public, forked 30 Aug 2026, default branch `develop`; never vendored into the monorepo). TDC Core API and Fastlane live in `platform/services/` — **core-api has not been split out**, despite discussion; if that changes, this line changes with it.
- **Domains:** api.thedoorstepclinic.com (Core) · e.thedoorstepclinic.com (Fastlane — short on purpose, NTAG URL budget) · clinic. (HMS) · console. (admin) · app. (web build). No new TLDs.
- **NFC:** NTAG 424 DNA, SDM signed URLs (`/e/{uid}?ctr&cmac`), CMAC verify + counter replay-kill, revocation = one `active` flag.
- **HARD-PROHIBITED (rejected architectures — do not reintroduce even if found in older docs):** Hyperledger/blockchain anything · chaincode · CDO specs · symmetric-AES-on-card schemes · card-tap-as-consent · iOS (Phase 2) · OCR/AI extraction · offline mode.

## Consent model (compliance-critical, do not drift)

- Consent is granted **once, explicitly, in-app, logged, revocable**. The NFC card is a capability token exercising a standing grant — never describe a tap as consent.
- ABDM consent is strictly per-patient; family layer = TDC's own `caregiver_grants`, not ABDM delegation.
- ABHA record inflow = ABDM **subscription** + patient-set auto-approve. See `specs/003-abdm-sync-subscription/spec.md`.

## Banned phrases (code comments, UI copy, docs — everywhere)

"auto consent" · "blockchain"/"Hyperledger" · cipher names in UI ("AES-256", "military-grade") · "ABDM certified" · "we fetch everything" · anything implying card-tap = consent.
**Approved copy:** "encrypted" · "stored only in India" · "every access is logged" · "you control who sees what" · "one-time permission" · "built on India's ABDM framework (integration in development)".

## Architecture

```
TDC Health (Flutter)    TDC Doctor (Flutter, planned)  future clients
   └────────────────────────┴──────────────────────────────┘
                            │ JWT — every app is a client, owns no tables
                            ▼
TDC Core API (Django/DRF + Postgres)  ←—(P1 webhook)— CARE fork HMS (TDC Clinic)
   │ writes denormalized emergency_payload snapshot on profile save
   ▼
Emergency Fastlane (FastAPI, separate uptime budget)
   └─ GET /e/{uid}?ctr&cmac → server-rendered responder HTML (zero JS) + FCM family blast + scan log
```

Fastlane reads the snapshot, never live Core joins — it must survive Core being down.

## Data model (prototype)

`users` · `profiles`(owner FK, relation) · `caregiver_grants`(grantor, grantee, ts, revoked_ts) · `records`(profile, type[Rx|Lab|Discharge|Other], title, file, record_date, source[manual_upload|tdc_hms]) · `medications`(profile, name, dose, times[], stock_count, threshold, status[active|stopped], started_on, ended_on) · `dose_events`(medication, profile, dose_date, slot_time, status[taken|skipped], marked_ts) · `emergency_profiles`(1:1, blood_group, allergies, conditions, abha_no) · `emergency_contacts`(profile, name, phone, relation, priority) · `cards`(uid, profile, sdm_key_ref, last_ctr, active) · `scan_events`(card, ctr, ts, ip, geo, is_test) · `appointments`(profile, clinic FK, doctor FK, slot FK, slot_ts, token_no, status[booked|cancelled|rescheduled|lapsed], cancelled_at, cancelled_by, reschedule_count, rescheduled_from, rescheduled_to, created_at) · `specialties`(name, icon_ref, sort_order, is_primary) · `clinics`(source[seed|tdc_clinic|uhi], external_ref, name, address_line, area, city, lat, lng, distance_km, hero_image_ref, rating, typical_wait_min, queue_count, consult_fee_inr, cancellation_window_min, facility_tags[], faqs, active) · `doctors`(source, external_ref, clinic, name, specialty, qualification, experience_years, hid_masked, is_lead, photo_ref, active) · `slots`(doctor, slot_ts, is_booked) · `access_logs`(actor_user, subject_profile, action[read|write], object_type, object_id, ts, purpose, source_service[core|fastlane]).

`access_logs` is required by constitution Principle XI — and independently by Principle VI, since "every access is logged" is already on the approved-copy list and the Trust screen must not claim it before the table exists. Distinct from `scan_events`: that records card mechanics (counter, replay state), this records who saw what. Keep both.

The four directory tables (`specialties`/`clinics`/`doctors`/`slots`) are **reference data, not PHI** — they are the app's only intentionally unscoped read path, and that is recorded in `specs/012-appointment-booking/checklists/security.md`. If a profile-derived field is ever added to them, Principle X applies and the exemption ends. They live in Core and are served to every client; `source`/`external_ref` are internal and never serialized out. `source = seed` rows are `DEMO_MODE` fixtures; production rows come from TDC Clinic (`tdc_clinic`) or UHI (`uhi`). **`slots.is_booked` is not availability** — availability is the clinic HMS's, and a local flag flip is not a reservation. The production path is a **booking request to TDC Clinic / UHI that can be declined**; no code may treat the flip as a confirmation. (GATE for the `uhi` source: UHI onboarding — owner Adi. `tdc_clinic` has no gate: we own the fork.)

**`appointments.status` says only what we actually know (`012`).** The full production enum is `requested · booked · declined · cancelled · rescheduled · completed · no_show`, with **`lapsed` derived at read time** (never stored, never swept by a job) for the window where a slot has passed and no clinic outcome has arrived. **`completed` and `no_show` are clinic-side writes only** — the patient app never asserts an outcome it did not observe: `completed` would claim an unwitnessed event and `no_show` is an accusation. Until the TDC Clinic accept/decline surface exists, `requested`/`declined`/`completed`/`no_show` stay unwritten and `lapsed` covers the gap — that is a `DEMO_MODE`-adjacent shortfall with a named owner, not a design. Copy rule is permanent regardless: **"Booking placed" / "Booked"**, never "confirmed", "reserved" or "held" until the clinic has actually acknowledged.

API base `/api/v1/`: auth/otp (mock OTP `000000` in demo mode) · profiles CRUD · profiles/{id}/records · profiles/{id}/summary.pdf · profiles/{id}/medications · profiles/{id}/emergency · cards link/revoke · clinics (search/filter) · clinics/{id} · clinics/{id}/nearby · clinics/{id}/doctors · doctors/{id}/slots (**returns taken slots too, flagged — `012` renders them struck through, not hidden**) · specialties · profiles/{id}/appointments · POST appointments · appointments/{id}/cancel · appointments/{id}/reschedule. Fastlane: GET /e/{uid} only.

## Screens (12)

Splash/Login · Home (family cards + alerts strip, nothing else — no feed, ever) · Profile timeline (filter chips, FAB→upload) · Upload (camera/gallery→crop→title+type+date) · Record detail · Health Summary (share-as-PDF prominent) · Meds list · Med add/edit · Emergency profile editor (+ "what responders see" preview) · Card manager (test scan, revoke, scan log) · Family & consent (grants with timestamps) · Trust screen (approved copy only).

**UX loops:** first-session = "Add family member" prompt + per-card setup checklist chips (records/meds/emergency/card); daily = alerts strip + reminder notifications (notification→done <10s); event = Summary-PDF share + card scan→family blast. Home stays quiet — that's the trust positioning. Reference apps: steal record-viewing patterns from Tata 1mg/Eka Care; never their commerce-feed home.

**Design:** warm/calm, site blue #4B83F2 family, large type + tap targets (elderly users), readable at arm's length. Tokens in `docs/design-tokens.md` (create if missing).

## Seed data (demo state — `seed_demo.py`, idempotent, one command)

1 user (Rohan 34), 3 profiles: Rohan(1 record) · **Asha 61** (10 records Jan25–Jul26: T2 diabetes arc HbA1c 8.1→6.9, penicillin allergy, Nov-25 hypoglycemia discharge; Metformin stock=4 → triggers low-stock alert; card linked) · Prakash 66 (3 records, hypertension, Amlodipine). All clinics fictional. Full contract in `specs/004-seed-data-and-summary/spec.md`. **Rule: any model change updates seed_demo.py same day.**

## Build order (D1–D12) & workflow rules

D1–2 skeleton+auth+profiles+seed v0 · D3–4 records · D5 summary PDF · D6–7 meds+reminders · D8–9 Fastlane · D10 card personalization+FCM e2e · D11 polish+APK · D12 rehearsal ×3 + backup video.

- **Vertical slices:** every session ends demo-able (screen→API→DB→seed in one pass).
- **Slip rule — REPEALED 6 Sep 2026.** No feature is cut to fund another (constitution v3.0.0, workflow rule 3). Principle I still means the emergency flow is never destabilised or reordered behind anything, but it is now a **stability** rule, not a triage rule. Sequencing is by **dependency**, one feature at a time, each to completion.
- **The P0/P1 ladder is gone with it.** Former P1 items are ordinary features with ordinary specs: HMS→app slice · ABHA M1 (sandbox creds arrived 17 Aug 2026) · Marathi responder toggle · test-scan celebration.
- **P1 booking scope note:** `012-appointment-booking` is the single owning spec for the whole `/book` sub-tree (B1–B7). `010` and `011` are superseded and retained only as decision records — do not implement from them.
- **Scope, restated 6 Sep 2026 (the old out-of-scope list was mostly track-driven and is repealed).** Three categories now:

  **In scope — build to production.** Payments (Razorpay UPI-first, locked stack; the 🔒 pay-at-clinic badge is no longer the ceiling) · real auth hardening (rate limiting, lockout, session/token handling, secret management) · clinic-side accept/decline of a booking (a booking must be a request that can be declined — `012` already models `requested`/`accepted`/`declined` as the seam) · real HMS queue feed from TDC Clinic (we own the CARE fork; the client-side simulation was a stand-in) · queue-position notifications, **once the feed is real** — never from a simulation.

  **Gated, not deferred — name the gate and its owner in the spec.** Real UHI provider directory (GATE: UHI onboarding) · ABDM M1/M2/M3 (GATE: WASA audit → NHA milestones; `docs/compliance-baseline.md`) · HFR/HPR provider registry lookups (GATE: same). Build everything on our side of the gate; claiming the integration before it exists is still a Principle VI violation.

  **Still prohibited — on grounds that never depended on the track split.** Everything in HARD-PROHIBITED above (blockchain, card-tap-as-consent, OCR/AI extraction, iOS, offline mode) · clinic browse destinations — no "See All", no services tab (Principle VIII) · doctor-app features in this repo (TDC Doctor is its own product directory).

  **Two items need an owner decision — they were rejected partly for reasons that just changed:**
  - **Symptom search.** Rejected 20 Aug 2026. Two of its three reasons stand independently of the track split: symptom→specialty is **medical inference** needing a curated mapping and a not-medical-advice disclaimer, and it puts **health complaints into search logs**. The third reason — "a compliance pass Track A has no room for" — is void. **Stays rejected until explicitly revisited**, but the blocker is now a compliance pass someone must own, not a capacity excuse.
  - **Written reviews / review counts / ratings-driven sort.** Rejected because fabricating a review corpus for fictional clinics is dishonest (Principle VI). With a real provider directory that objection disappears and a genuine post-visit feedback loop becomes possible. **Stays out until an owner decides**, because it is a product and moderation commitment, not a leftover fence.

## Working conventions

- Solo dev (Adi), ~4h/day. Prefer boring/already-paid-for. Estimates must respect this ceiling.
- Adversarial review habit: anything touching auth, grants, or Fastlane gets a "how would I abuse this?" pass before merge.
- **Compliance is enforced at generation time, not review time.** Constitution **v3.0.0** — Principles I–XV, **all binding, no classes**. The `[A]`/`[B→A]`/`[B]` markers are removed; a marker surviving in any doc is stale, not a live exemption. The three that bite hardest at code-generation time are unchanged in substance: **X** (every queryset scoped server-side from `caregiver_grants` — unscoped `objects.all()` is a build failure), **XI** (`access_logs` row per PHI access, same transaction), **XIV** (every `# DEMO-MODE` tag names its real path, and an unbuilt real path names its blocking gate **and that gate's owner**, or it is a defect). Features touching PHI/auth/grants/Fastlane/ABDM carry `specs/NNN-slug/checklists/security.md`; ABDM surfaces also carry `abdm.md`. Templates in `.specify/templates/`.
- **`DEMO_MODE` discipline (Principle IV).** One switch, off in every build leaving a dev machine. It substitutes **data**, never **behaviour** — it may return fixture rows; it may not skip authorization, skip logging, weaken validation, or produce a state the real system cannot. If removing it breaks a feature, that feature is unfinished. `seed_demo.py` stays a first-class dev/test fixture and is never shipped as content.
- External compliance *facts* (milestone definitions, WASA scope, Fidelius construction, FHIR IG version pin, DPDP dates) live in `docs/compliance-baseline.md` with sources and a verification date — **not** in the constitution, so version pins rot in one place. Verified 10 Aug 2026. **FHIR pin: R4.0.1 + NRCeS IG v6.5.0 released; v7.0.0 is draft — do not build against it.**
- Docs live in `specs/` (Spec Kit format); this file gets a one-line changelog entry on any locked-decision change.
- **Repo layout is decided, in `docs/repo-structure.md`:** services are **platform-level, not inside `health/`** — `platform/services/core-api/` · `platform/services/fastlane/` (moved out 20 Aug 2026, because a service the doctor app calls cannot live inside the patient app's product directory). `health/` now holds `apps/health/` plus its specs and docs, and is scoped to **the patient client**, not the patient stack. Path-filtered CI with **independent per-service deploys** (a shared pipeline would couple Fastlane's uptime to Core's and make Principle I fiction). No workspace tooling for Track A; no shared Python between the two services — they share a *table*, not a codebase. TDC Doctor and TDC Clinic are each their own top-level product directory (`platform/doctor/`, `platform/clinic/`); TDC Clinic's CARE-fork source additionally stays in its own separate repo entirely (`thedoorstepclinic/tdc-care`), never copied in anywhere — a vendored fork cannot cleanly merge from an actively developed upstream. `platform/clinic/` exists as the specs-and-docs stub (created 30 Aug 2026).
- **Every app is a client (20 Aug 2026, locked):** TDC Core API owns the data. TDC Health, TDC Doctor, and any future client consume it over HTTP and hold **no tables of their own** — no client-local directory table, no local cache presented as data. Directory responses are client-agnostic: no field varies by which app asked, and a different projection is a new endpoint, not a branch inside an existing one. Fastlane is not a counterexample — it is a *reader* of a snapshot table, deliberately not a client (Principle I). Track B, Core becomes the **aggregation point**: it ingests TDC's own facilities from TDC Clinic and external providers from UHI/HFR/HPR and still serves one directory API out.
- Real requirements without a spec yet are staged in `docs/backlog.md` (account recovery, data export, app lock, app-wide Marathi, home vitals logging, Help & Support) — check it before "discovering" a gap. **All of it is in scope**; an entry leaves that file by becoming a `specs/NNN-slug/` directory, not by being reclassified. **Account recovery (§1) is the highest-severity unspecced item** — phone *is* the account, Indian users churn SIMs, and a lost number would mean a permanently lost family health record.
- Stale-doc hygiene: if you encounter Hyperledger-era content anywhere, flag it for deletion — do not incorporate it. Same standard applies to real facility/brand names leaking into seed data or example copy — flag and replace with a fictional one (see `specs/004-seed-data-and-summary/spec.md`).

## Changelog

- 16 Jul 2026: initial pack. Names locked (TDC Health etc.), package IDs locked, UX loops added, seed contract referenced.
- 16 Jul 2026: placed at repo root; doc pointers corrected from the originally-drafted flat `/docs/*.md` layout to this repo's actual Spec Kit `specs/NNN-slug/spec.md` structure. Caught and fixed a real-facility-name leak ("Ruby Hall Clinic," an actual Pune hospital) in example push-copy across `001` and `003` — replaced with the fictional "Ashirwad Diagnostics," now the standard example lab name. Added `004-seed-data-and-summary` spec.
- 16 Jul 2026: `005-records-capture-timeline` (multi-page scan via ML Kit, no OCR) and `006-medications-reminders-adherence` (bundled dose-occasion checklist) specced. Model change from `006`: `medications` gains `status`/`started_on`/`ended_on`; new `dose_events` table (confirmation-driven stock depletion). Seed reconciled — Asha now correctly has 2 active meds (Metformin + Sitagliptin, both at 20:00) per `001` §2.
- 16 Jul 2026: `007-emergency-profile-card-consent` specced (screens 9–11 + abuse pass). Decisions: one active card per profile (revoke-then-replace) · test scan = real Fastlane round-trip via single-use server-minted URL, blast labeled as test, chip counter untouched. Model change: `scan_events` gains `is_test`. Preview rule locked: "what responders see" renders from the same `emergency_payload` snapshot Fastlane serves — never a parallel template.
- 16 Jul 2026: `/docs/design-tokens.md` created (was "create if missing"). `008-navigation-app-shell` (route map, hub-and-spoke nav — no bottom tab bar, deep-link contract, four screen states, no-offline error conventions) and `009-utility-screens` (Login, Health Summary screen locked to `004`'s PDF mapping, Trust screen full copy structure, minimal Settings) specced. Every screen in the app now has an owning spec.
- 1 Aug 2026: **Owner decision (Adi): consult booking + queue tracking unlocked** — removed from the banned list after explicit (non-casual) revisit. Rationale: pitch date confirmed as 16 Aug 2026 (runway grew ~2 weeks past the old end-July target, funding the build without cutting existing scope), near-term audience is meetups/social demos needing breadth and a "how does this make money" answer. Strictly **seeded/demo-grade** (`010-consult-booking-queue`): fictional clinics only, queue is a client-side simulation, 🔒 pay-at-clinic, no real UHI calls (UHI named as Track B rails). Investor golden path (`001`) unchanged — booking is the meetup loop's beat, not the pitch script's. New table: `appointments`. Real UHI integration + real-time HMS queue feeds added to the banned list in its place.
- 10 Aug 2026: **compliance readiness wired into the spec pipeline.** Constitution → v2.0.0 (MAJOR: scope redefined from "Track A only" to per-principle binding classes) with Principles X–XV covering ABDM M1/M2/M3, WASA, and DPDP. Researched requirements captured in `/docs/compliance-baseline.md`; checklist templates added at `.specify/templates/checklist-{security,abdm}.md`; `plan-template.md`'s Constitution Check gate corrected (it had gone stale at Principle VII, silently skipping VIII and IX) and extended with the checklist gate. **Model change: new `access_logs` table** — the one Track A build cost, justified because approved copy already promises it. Correction to a widely-repeated claim: WASA is a **precondition for M1**, not a post-M3 step, and the NRCeS IG pin is v6.5.0 *released* with v7.0.0 in draft since 15 Jul 2026 — the pin is dated, not permanent. Track A scope is unchanged: no ABDM exchange, no Fidelius, no FHIR emitted.
- 10 Aug 2026: repo layout committed to `/docs/repo-structure.md` (was decided in chat, never written down) — three-unit monorepo, per-service deploys, no shared Python across the service boundary, npm workspaces (not pnpm) if/when TDC Doctor lands, shared palette but *not* shared type scale. Gap list committed to `/docs/track-b-backlog.md` (renamed `docs/backlog.md`, 6 Sep 2026). No locked decision changed; both files record decisions that were already made.
- 15 Aug 2026: **Owner decision (Adi): stack changed from Expo React Native to Flutter.** NativeWind and "FCM via Expo push" drop with it — push goes direct FCM. Repo migrated into the `platform` monorepo as a self-contained `health/` product directory (own CLAUDE.md, specs, docs, .specify) — `.claude/skills` and `.claude/agents` stay shared at the monorepo root, everything else product-specific lives under `health/`. **`health/` is patient-app-scoped only** — the app directory is `health/apps/health/`. TDC Doctor is **not** nested under `health/`; it is its own self-contained top-level product directory at `platform/doctor/` (same pattern as `health/`, built when it's actually started — currently an empty placeholder). `005`/`006`/`001`'s plan/research/tasks/quickstart, `README.md`, `design-tokens.md`, and `.specify/memory/constitution.md` still describe the old Expo/RN stack and need a pass to match — not yet done.
- 17 Aug 2026: **Stack-migration cleanup pass.** `001`'s plan/research/data-model/contracts/quickstart/tasks fully repointed to Flutter + the `health/apps/`+`health/services/` layout (was left half-done on 15 Aug); two previously-undesigned Track-A-binding constitution principles (X authorization scoping, XI access logging) now have a concrete design wired into `001`. Found and fixed additional Expo/NativeWind residue the 15 Aug entry didn't catch: `005`'s ML Kit scan build-note (was `react-native-document-scanner-plugin`/EAS dev-client language — Flutter has no Expo-Go/dev-client split), `006`'s reminder mechanism (now `flutter_local_notifications`), and `008`'s entire route map (was an `expo-router` file tree — now a `go_router` route tree; **`go_router` is a new routing-library decision this pass introduced**, not a prior lock, flag if a different router is preferred). `.specify/memory/constitution.md` was already self-corrected in its own 2026-08-15 v2.0.1 patch. `README.md` and `design-tokens.md` checked this pass and found already stack-agnostic — no Expo/NativeWind/React Native references, nothing to fix. **Owner update: ABDM sandbox credentials arrived** — the "only if sandbox creds arrive" condition gating `001`'s P1 ABHA M1 (create-via-Aadhaar) is resolved; ABHA M1 is buildable if P0 time allows, still cut-bottom-up P1. `003`'s Track B prereq list noted the credential half is satisfied (its other prereqs — Sprint 1 tenant-isolation audit, pyfidelius sandbox validation, webhook receivers — are unaffected and still gate it).
- 20 Aug 2026: **`011-care-discovery` specced — booking gains a front end.** `010` shipped a bare 3-clinic picker; a stranger at a meetup asks "can I find a doctor near me, and is this place any good?", and a form doesn't answer that. Added a find-care screen (search over clinic/doctor/specialty names, specialty chips, distance-sorted clinic cards) and a clinic information page (hero, identity card, location + map, "other clinics nearby" rail, Facilities/FAQ tabs, sticky Book CTA), both drawn from reference designs supplied by Adi. **Principle VIII was preserved without amendment** — discovery is reachable only from a profile's action row, has no Home entry and no nav slot, and the nearby rail deliberately ships without a "See All" so it stays navigation rather than a browse destination. **Flow change: `010` FR-002 amended** from clinic→slot→confirm to find care→clinic→doctor→slot→confirm (3 taps → 5). **Model change: four new tables** (`specialties`, `clinics`, `doctors`, `slots`) and `appointments`' copied strings become FKs — this creates the app's first intentionally unscoped read path, non-PHI reference data, recorded in `011`'s security checklist with a regression guard against a future profile-derived field turning it into a leak. **Three owner overrides**, each recorded with a written mitigation rather than a silent pass: seeded star **ratings** kept (mitigation — rating only, no review count, no review text, no Reviews tab, no sort-by-rating); the doctor **HID chip** kept (mitigation — no verification/ABDM/HPR/registry copy, no tick iconography, no tap affordance; "ABDM certified" remains banned); **real map + Directions** on seeded coordinates kept (mitigation — coordinates must not land on a real healthcare facility, and the maps API key must be package+fingerprint restricted before any APK ships). **Rejected: symptom search** — added to the out-of-scope list. `004`'s fictional-facility guardrail extended from names to **imagery and coordinates**, and ~7 new clinic names are blocked on Adi's real-Pune check before the seed lands.
- 20 Aug 2026: **Owner decision (Adi): every app is a client; services move to `platform/services/`.** `011` raised the question — where does the doctor directory live? — and the answer settled a platform rule that had been implicit since the monorepo landed: **TDC Core API owns the data, and TDC Health / TDC Doctor / anything later are clients that own no tables.** A clinician app "obviously" needs its own doctor table, and the day it has one there are two answers to who Dr. Kavya is; the rule is now written in `platform/README.md`, `doctor/README.md` (before a line of its code exists) and `docs/repo-structure.md`. **Layout change:** `services/core-api/` and `services/fastlane/` moved out of `health/` to `platform/services/` — `health/` was documented "patient-app-scoped only", which stopped being true once Core served the doctor app too. Free to do now, expensive later: nothing is scaffolded. `001`'s quickstart/tasks/plan paths repointed (11 references); the 17 Aug changelog entry above is left as written history and is superseded on the services half only. **Track B model:** Core aggregates rather than authors — TDC Clinic for own facilities, UHI/HFR/HPR for external — so `clinics`/`doctors` carry `source` and `external_ref` from day one. That also makes the HID chip coherent: today an invented string, in Track B a masked HPR `external_ref`, so the 20 Aug override survives the transition instead of being deleted in it. **Not decided:** whether core-api eventually gets its own git repo (Adi raised it). Held for Track B and parked in `011`'s Open Decisions — core-api is the epicentre of the Core→Fastlane→seed→screen ripple that justified the monorepo, so splitting it out is the single most expensive split during a 4h/day build.
- 30 Aug 2026: **TDC Clinic's CARE fork created — `thedoorstepclinic/tdc-care`** (public, forked from CARE, default branch `develop`). No architectural change: the locked decision already said the CARE-fork source lives in its own repo and is never copied in, so this only records the name and URL. `platform/clinic/` created as the specs-and-docs stub `docs/repo-structure.md` had promised but never had. Recorded in `platform/README.md`, `docs/repo-structure.md`, and the new locked **Repos** line above. Two integration points from the Core side are already specced and unchanged: the P1 HMS→app Rx slice (`001`) and Track B directory/queue ingest (`011`, `source = tdc_clinic`) — Track A depends on neither. **Correction to the working assumption during this session:** `tdc-care` was momentarily taken to be core-api split into its own repo; `gh repo view` showed `isFork: true` with CARE's description and branch, so it is the HMS fork. **TDC Core API remains in the monorepo at `platform/services/core-api/`** — the split is still an open decision parked in `011`, for the reason given there (core-api is the epicentre of the Core→Fastlane→seed→screen ripple the monorepo exists to keep cheap). **Note:** the fork is public, which is normal for an open-source fork; revisit visibility before TDC-specific config, deployment manifests, or facility data are committed to it.
- 6 Sep 2026: **`012-appointment-booking` specced — `010` + `011` merged into one owning spec.** Booking was split across two specs with a seam through the middle (`011` owned three screens, `010` owned four) and the appointment's life *after* confirmation was owned by neither: reschedule did not exist, cancellation had a verb and no policy, nothing scheduled a reminder. `012` takes B1–B7 end to end; `010`/`011` are **superseded, retained as decision records** — `010`'s 1 Aug unlock provenance and `011`'s eight 20 Aug decisions (including the three owner overrides) still bind, and every FR from both is mapped forward in `012` §Carried forward. **Owner clarifications (6 Sep):** Track A, seeded, fences unchanged · no clinic accept/decline · reschedule + cancel-window + reminders in scope · payments specced-but-not-built · **the ~7 fictional clinic names land as-is**, unblocking `004`'s seed (residual risk recorded there: accepted on judgement, not a register check; the avoid list stays binding). **The load-bearing decision is honesty about state.** With no clinic surface, nothing observes attendance — so the app never writes `completed` (asserting an unwitnessed event) or `no_show` (an accusation). Track A has one derived post-time state, **`lapsed`**, computed at read time with no background job to run mid-demo; `requested`/`declined`/`completed`/`no_show` are named in the enum as Track B clinic-side writes so no Track A state is renamed later. Same rule in copy: **"Booking placed" / "Booked", never "confirmed"/"reserved"/"held"** — `011` warned that `slots.is_booked` is a local flag the clinic has never seen, and `012` turns that warning into a design rule. **Model changes:** `appointments` rewritten (`status` enum, `cancelled_at`, `cancelled_by`, `reschedule_count`, `rescheduled_from`/`rescheduled_to`, `created_at`); `clinics` gains `consult_fee_inr` and `cancellation_window_min` (per-clinic, so Track B varies it without a migration). **API:** `GET /doctors/{id}/slots` amended to return taken slots flagged rather than filtered — an empty morning column reads as "closed in the mornings", a false impression created by omission, and a grid that reflows as others book moves under the user's thumb. Plus `POST /appointments` (409 on a taken slot — two phones at a meetup must not double-book), cancel, and reschedule. **Policies set:** cancel/reschedule cutoff = `cancellation_window_min` (120 default, one clinic seeded 240 to exercise the path); **late cancellation stays allowed** because blocking it produces a no-show, which is worse for the clinic; **no no-show consequence in Track A** since we cannot detect one; reschedule capped at 2, same doctor only, new row + fresh token (a token is a position in one session's queue). **Reminders reuse `006`'s `flutter_local_notifications` primitive** at T−24h/T−2h — one scheduler, one stop-and-new code path, ids namespaced so an appointment move cannot silently cancel an 8pm dose reminder. **No queue-position push** — added to the banned list. **Alerts strip bounded** (`001` §4.1): ≤1 row per appointment, 48h window, ≤2 rows total, never above low-stock — without a cap a caregiver with three parents turns the strip into the feed Principle VIII exists to prevent. **Reason-for-visit cut** — nobody reads it in Track A, so it would collect a health complaint for no purpose. **Payments shaped, not built:** seeded fee displayed with the 🔒 pay-at-clinic badge; Track B's Razorpay/refund/failure states specified to live on a separate `payments` table so a gateway failure cannot corrupt an appointment's lifecycle. **DEMO-MODE tags 4 → 7** (adds seeded fees, `slots.is_booked`, derived `lapsed`). Ripples applied same day: `008` (route map — added `/appointment/:id/reschedule`, retagged the sub-tree, deep-link rows + a dead-link rule, **and deleted the stale `/who-for` route `002`'s Aug rewrite had already removed**), `004` (fee/window/30%-pre-booked seed + names unblocked), `006` (shared-primitive note), `001` (bounded alerts row), and `012/checklists/security.md`.
- 6 Sep 2026: **Owner decision (Adi): the Track A / Track B split is abolished. One project, built to production, every feature a priority with its own space.** This is the largest governance change since the constitution was written, and it touches 32 files. **Constitution → v3.0.0 (MAJOR).** The *Binding Classes* section is deleted and the `[A]` / `[B→A]` / `[B]` markers are stripped from Principles X–XV — **all fifteen principles now bind unconditionally**, and "Track B" is no longer a valid deferral target: a feature blocked by an external gate names **the gate and its owner** instead. A marker surviving in any document is a stale doc, not a live exemption. **Principle IV was inverted** — from *"Demo-Grade, Not Production-Grade — Stated Honestly"* to *"Production-Grade, With Demo Data Switchable"*. The prototype survives as a single `DEMO_MODE` switch, off in every build that leaves a dev machine, which may substitute **data** but never **behaviour** (never skips authorization, logging or validation, never invents a state the real system cannot produce) and may never be the only path — **if removing it breaks a feature, the feature is unfinished**. `seed_demo.py` stays a first-class dev/test fixture, never shipped content, which is also why Principle V survives on new grounds: an unresettable feature is an untestable one. **Principle XIV was reframed** from a remediation list bequeathed to a later track into a **burn-down**: a demo tag must name its real path, and an unbuilt real path must name its blocking gate and that gate's owner, or it is a defect. Security posture moved prototype → production, repealing *"full auth hardening is explicitly Track B"*. **Workflow rule 3 repealed the P0/P1 ladder and the cut-from-the-bottom slip rule**; sequencing is now by dependency, and Principle I becomes a **stability** rule (the emergency flow is never destabilised or reordered behind anything) rather than a triage rule. New workflow rule 7: every feature owns a complete spec-kit set — only `001` currently does. **Capacity is unchanged at ~4h/day solo**, so this raises the quality bar without raising throughput: "every feature is a priority" means none is cut to fund another, *not* that everything is built at once — features ship in dependency order, one at a time, each to completion. **Scope list rewritten** into three categories: *in scope* (payments via the already-locked Razorpay UPI-first, real auth hardening, clinic-side accept/decline, real TDC Clinic queue feed, queue notifications once the feed is real), *gated* (UHI directory, ABDM M1–M3, HFR/HPR lookups — each naming its gate), and *still prohibited on independent grounds* (the HARD-PROHIBITED list, clinic browse destinations per Principle VIII, doctor-app features in this repo). **Two items flagged for an owner decision** because they were rejected partly for reasons that just became void: **symptom search** (two of three objections stand — medical inference, and health complaints in search logs — but "no room in Track A" does not; stays rejected pending an owned compliance pass) and **written reviews** (the fabricated-corpus objection dissolves with a real directory; stays out pending a product and moderation decision). Supersedes `002`'s 18 Aug governance clause (*"tracks stay separate… the constitution keeps its per-principle binding classes"*), now void. **Ripple not yet complete** — root documents (constitution, this file) are done; the 30 remaining files carrying Track A/B language, binding-class markers, or demo-grade framing are tracked in the sweep below.
